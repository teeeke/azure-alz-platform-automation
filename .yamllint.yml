# This is a basic YAML lint configuration file
extends: default

rules:
  line-length: disable
  comments: disable
  document-start: disable
  truthy:
    check-keys: false
  brackets:
    min-spaces-inside: 0
    max-spaces-inside: 1
  indentation:
    spaces: 2
    indent-sequences: consistent
